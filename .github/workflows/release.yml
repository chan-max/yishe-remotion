name: Build Desktop Installers and Release

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/release.yml"
      - "package.json"
      - "package-lock.json"
      - "scripts/release.ts"
      - "server/**"
      - "templates/**"
      - "remotion/**"
      - "ui/**"
      - "public/**"
      - "tsconfig.json"
      - "remotion.config.ts"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: desktop-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      should_release: ${{ steps.version.outputs.should_release }}
      version: ${{ steps.version.outputs.version }}
      release_tag: ${{ steps.version.outputs.release_tag }}
      release_name: ${{ steps.version.outputs.release_name }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Resolve release version
        id: version
        shell: bash
        run: |
          VERSION=$(node -p "require('./package.json').version")
          RELEASE_TAG="v${VERSION}"
          RELEASE_NAME="Desktop Installers ${RELEASE_TAG}"

          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "release_tag=${RELEASE_TAG}" >> "$GITHUB_OUTPUT"
          echo "release_name=${RELEASE_NAME}" >> "$GITHUB_OUTPUT"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "should_release=true" >> "$GITHUB_OUTPUT"
            {
              echo "## Desktop release"
              echo ""
              echo "Manual dispatch requested. Building installers for \`${RELEASE_TAG}\`."
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          PREVIOUS_VERSION=$(git show HEAD~1:package.json 2>/dev/null | node -e "const fs = require('node:fs'); const text = fs.readFileSync(0, 'utf8'); console.log(JSON.parse(text).version);" 2>/dev/null || echo "none")

          if [ "${VERSION}" = "${PREVIOUS_VERSION}" ]; then
            echo "should_release=false" >> "$GITHUB_OUTPUT"
            {
              echo "## Desktop release skipped"
              echo ""
              echo "package.json version unchanged: \`${VERSION}\`."
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          echo "should_release=true" >> "$GITHUB_OUTPUT"
          {
            echo "## Desktop release"
            echo ""
            echo "Version changed: \`${PREVIOUS_VERSION}\` -> \`${VERSION}\`."
            echo "GitHub release tag: \`${RELEASE_TAG}\`."
          } >> "$GITHUB_STEP_SUMMARY"

  build:
    needs: prepare
    if: needs.prepare.outputs.should_release == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: windows-x64
          - os: macos-13
            label: macos-x64
          - os: macos-14
            label: macos-arm64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.22

      - name: Install dependencies
        run: bun install

      - name: Install NSIS
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install nsis -y --no-progress

          $programFilesX86 = ${env:ProgramFiles(x86)}
          $chocoInstall = if ($env:ChocolateyInstall) { $env:ChocolateyInstall } else { "C:\ProgramData\chocolatey" }

          $candidateDirs = @(
            (Join-Path $chocoInstall "bin"),
            (Join-Path $chocoInstall "lib\nsis\tools"),
            $(if ($programFilesX86) { Join-Path $programFilesX86 "NSIS" } else { $null }),
            $(if ($env:ProgramFiles) { Join-Path $env:ProgramFiles "NSIS" } else { $null })
          ) | Where-Object { $_ -and (Test-Path $_) }

          foreach ($candidateDir in $candidateDirs) {
            Add-Content -Path $env:GITHUB_PATH -Value $candidateDir
          }

          foreach ($candidateDir in $candidateDirs) {
            if ($env:Path -notlike "*$candidateDir*") {
              $env:Path = "$candidateDir;$env:Path"
            }
          }

          $makensis = @(
            (Join-Path $chocoInstall "bin\makensis.exe"),
            (Join-Path $chocoInstall "lib\nsis\tools\makensis.exe"),
            $(if ($programFilesX86) { Join-Path $programFilesX86 "NSIS\makensis.exe" } else { $null }),
            $(if ($env:ProgramFiles) { Join-Path $env:ProgramFiles "NSIS\makensis.exe" } else { $null })
          ) | Where-Object { Test-Path $_ } | Select-Object -First 1

          if (-not $makensis) {
            $makensis = (Get-Command makensis.exe -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Source -First 1)
          }

          if (-not $makensis) {
            throw "NSIS install step completed, but makensis.exe was not found."
          }

          "MAKENSIS_PATH=$makensis" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "NSIS_HOME=$(Split-Path -Parent $makensis)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          & where.exe makensis

      - name: Build desktop installer
        env:
          RELEASE_VERSION: ${{ needs.prepare.outputs.version }}
          RELEASE_TAG: ${{ needs.prepare.outputs.release_tag }}
        run: bun run release

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.label }}
          path: release/dist/artifacts/*
          if-no-files-found: error

  release:
    needs:
      - prepare
      - build
    if: needs.prepare.outputs.should_release == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Download installer artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ needs.prepare.outputs.release_name }}
          tag_name: ${{ needs.prepare.outputs.release_tag }}
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          files: |
            release-assets/**/*

      - name: Release summary
        shell: bash
        run: |
          REPO_URL="https://github.com/${{ github.repository }}"
          {
            echo "## Desktop installers published"
            echo ""
            echo "Tag: \`${{ needs.prepare.outputs.release_tag }}\`"
            echo ""
            echo "Fixed latest download URLs:"
            echo ""
            echo "- Windows x64: ${REPO_URL}/releases/latest/download/yishe-video-tool-windows-x64-setup.exe"
            echo "- macOS Intel: ${REPO_URL}/releases/latest/download/yishe-video-tool-macos-x64.dmg"
            echo "- macOS Apple Silicon: ${REPO_URL}/releases/latest/download/yishe-video-tool-macos-arm64.dmg"
            echo "- Windows x64 plugin bundle: ${REPO_URL}/releases/latest/download/yishe-remotion-windows-x64-plugin.zip"
            echo "- macOS Intel plugin bundle: ${REPO_URL}/releases/latest/download/yishe-remotion-macos-x64-plugin.zip"
            echo "- macOS Apple Silicon plugin bundle: ${REPO_URL}/releases/latest/download/yishe-remotion-macos-arm64-plugin.zip"
            echo ""
            echo "Each release also uploads matching runtime manifests and SHA256 files."
          } >> "$GITHUB_STEP_SUMMARY"
